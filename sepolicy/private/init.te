allow init abm_file:dir mounton;
allow init abm_file:filesystem { unmount mount relabelfrom relabelto };
allow abm_file self:filesystem associate;

allow init system_security_cacerts_file:file mounton;
